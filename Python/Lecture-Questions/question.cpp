#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int n = 8;
    string arr[n] = {"but", "soft", "what", "light", "in", "yonder", "window", "breaks"};
    for (int i = 0; i < n; i++)
    {
        
    }

    return 0;
}